"""Placeholder tests for the restructured suite."""

import pytest


@pytest.mark.skip("Test suite pending rewrite after Phase 0 cleanup.")
def test_placeholder() -> None:
    """Fail early if placeholder still present after test migration."""
    assert False, "Replace with real tests once Phase 0 tasks complete."
